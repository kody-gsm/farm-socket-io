import adafruit_dht
import board
import time
import RPi.GPIO as GPIO

while True:
	try:
		h = adafruit_dht.DHT11(board.D4)
		print(h.temperature)
		time.sleep(10)
	except RuntimeError as error:
		print(error)
		time.sleep(2.0)
